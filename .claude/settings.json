{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_response.filePath // .tool_input.file_path' | { read -r f; case \"$f\" in *.ts|*.tsx|*.js|*.jsx|*.json|*.jsonc|*.css) ./node_modules/.bin/biome check --write \"$f\";; esac; } 2>/dev/null || true",
            "timeout": 30,
            "statusMessage": "biome formatting..."
          }
        ]
      }
    ]
  }
}
